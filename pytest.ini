[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    blender: renders through headless Blender/Cycles on the GPU (slow; deselect with -m "not blender")
filterwarnings =
    ignore::SyntaxWarning
